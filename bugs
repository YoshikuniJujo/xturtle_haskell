* dot to (0, 0) at first (fixed)
* dot when clear (fixed)
* dot when change shape, size, dir (fixed)
* clear timing too early (fixed)
* can't undo for clear (fixed)
* closeField don't work (fixed)
* redraw error when ExposeEvent occur (fixed)
* freeze ghci if closeField before turtle stop (fixed but not so good)
* flicker when undo (fixed)
* change pensize and undo (fixed)
* closeField, openField and newTurtle then turtle at wrong center
