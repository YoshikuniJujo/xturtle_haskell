* refactoring
	-- Input.hs
	-- Turtle.hs

Functions
* bgcolor may should be Turtle -> Color -> IO () instead of Field -> ...
	-- Because change bg color is drawing element
	-- How do I make in SVG <- draw square and draw rest
* fill
(* save as png by pixmap)
* get Mouse move event
(* get X events)
* get timer event
* raise exception if bad shape name
* scrolling

Bugs
* repair closeField
* direction should be independent from degrees

?
* How can I set Chan buffer size
